{
  "name": "tx-gun",
  "version": "1.0.0",
  "description": "An utility for blockchain performance testing. It allows to send multiple transactions to a blockchain node and analyze their minting.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Evgenii Zaitsev",
  "license": "MIT",
  "devDependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^5.1.3"
  },
  "dependencies": {
    "axios": "^1.4.0",
    "csv-stringify": "^6.4.0",
    "ethers": "^6.5.1"
  }
}
